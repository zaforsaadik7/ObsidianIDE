rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Authenticated users have read and write access to workspaces and collections
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
