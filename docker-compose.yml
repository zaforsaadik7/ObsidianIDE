version: '3.8'

services:
  obsidian-ide:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: obsidian_ide_app
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    restart: unless-stopped
